# AI Agent 协作约束

## 受保护功能：角色碰撞体与摄像头

本项目已统一离线玩家与联机本地角色的摄像头定位和防穿模行为。相关实现位于：

- `scripts/player.gd`
- `scripts/network_actor.gd`
- `scripts/game.gd`
- `scripts/skin_api.gd`
- `skins/*/skin.json`

必须保持以下行为：

1. 摄像头位置以实际 `CollisionShape3D` 为基准，不以视觉模型或角色根节点的固定高度为基准。
2. 第一、第三人称的实际 `Camera3D` 高度都不得高于当前碰撞形状最高点，并保留 `CAMERA_TOP_CLEARANCE`。
3. 胶囊顶部按 `collision.position.y + CapsuleShape3D.height / 2` 计算；`height` 已包含上下半球，不得再次加 `radius`。
4. `CapsuleShape3D`、`BoxShape3D`、`SphereShape3D` 和 `CylinderShape3D` 必须使用各自真实尺寸计算顶部。
5. 高度限制必须在摄像头球形扫掠、射线回退和最终重叠检查之前应用，不能绕过防穿墙检测。
6. `player.gd` 与 `network_actor.gd` 必须保持行为一致；联机远端角色不得创建活动摄像头。
7. 不得静默恢复基于 `Vector3(0.0, 1.55, ...)` 等角色根节点的硬编码摄像头高度。

## 修改前必须报告

任何 AI agent 若准备修改、删除、覆盖或重构上述行为，必须先向用户报告并等待用户检查、明确同意后再执行。报告至少包含：

1. 将修改的文件、函数和常量。
2. 修改前后的碰撞体顶部与实际摄像头高度。
3. 对第一人称、第三人称、离线和联机行为的影响。
4. 是否改变球形扫掠、射线回退、重叠检测或自身 RID 排除。
5. 是否仍满足摄像头高度不超过碰撞体最高点。
6. 拟执行的验证步骤。

如果合并、解决冲突或接受远端修改会覆盖本功能，也必须停止自动覆盖，向用户报告具体差异并请求检查。不得仅因 Git 未产生冲突就默认接受行为变化。
