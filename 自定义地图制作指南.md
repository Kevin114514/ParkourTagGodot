# 跑酷抓人：自定义地图制作指南

## 1. 现在的地图格式

地图已经改成 **可导入 JSON 地图**，并且游戏内有官方地图选择页面。

当前官方地图文件：

- `res://maps/default_arena.json`：默认跑酷竞技场
- `res://maps/ring_training.json`：圆环障碍训练场

在标题界面点击 **选择官方地图**，即可从官方地图列表中选择本局地图。联机房间里也可以由房主选择地图，客户端会自动同步。

打包版仍然可以放外部地图用于测试：

```text
maps/current_map.json
```

但正式游玩推荐把地图加入官方列表。联机时双方必须使用同一个打包版本，否则碰撞和位置会不同步。

---

## 2. JSON 基本结构

```json
{
  "format_version": 1,
  "name": "我的自定义地图",
  "runner_spawn": [-23.0, 0.12, 22.0],
  "tagger_spawn": [23.0, 0.12, -22.0],
  "objects": []
}
```

字段说明：

- `name`：地图名，游戏 HUD 会显示。
- `runner_spawn`：逃跑者出生点。
- `tagger_spawn`：抓人者出生点。
- `objects`：地图物体数组。

坐标仍然是 Godot 3D 坐标：

- `x`：左右
- `y`：高度
- `z`：前后

---

## 3. 支持的地图物体

### 3.1 盒子 / 地面 / 墙 / 斜坡

```json
{
  "type": "box",
  "name": "Ramp",
  "position": [0.0, 1.2, 0.0],
  "size": [8.0, 0.5, 10.0],
  "rotation_degrees": [18.0, 0.0, 0.0],
  "color": [0.35, 0.38, 0.42]
}
```

常用字段：

- `position`：中心点位置。
- `size`：宽、高、长。
- `rotation_degrees`：角度旋转，单位是度。
- `color`：RGB，范围 `0.0 ~ 1.0`。

斜坡仍然推荐 `10° ~ 25°`，太陡会影响角色移动和 AI 追人。

### 3.2 球体

```json
{
  "type": "sphere",
  "name": "BallObstacle",
  "position": [6.0, 1.0, 0.0],
  "radius": 1.0,
  "color": [0.5, 0.5, 0.8]
}
```

### 3.3 圆柱

```json
{
  "type": "cylinder",
  "name": "RoundPillar",
  "position": [-6.0, 1.5, 0.0],
  "radius": 0.8,
  "height": 3.0,
  "color": [0.45, 0.45, 0.5]
}
```

### 3.4 胶囊体

```json
{
  "type": "capsule",
  "name": "CapsuleObstacle",
  "position": [0.0, 1.4, 8.0],
  "radius": 0.6,
  "height": 2.8,
  "color": [0.2, 0.6, 0.5]
}
```

### 3.5 导入模型

支持：

- `.glb`
- `.gltf`
- `.tscn`
- Godot 可加载的 `Mesh` 资源

示例：

```json
{
  "type": "model",
  "name": "ImportedBuilding",
  "path": "models/building.glb",
  "position": [0.0, 0.0, -12.0],
  "rotation_degrees": [0.0, 90.0, 0.0],
  "scale": [1.0, 1.0, 1.0],
  "collision": "mesh"
}
```

`path` 可以写：

- 相对地图文件的路径：`models/building.glb`
- 项目资源路径：`res://models/building.glb`
- 用户目录路径：`user://maps/models/building.glb`

模型碰撞模式：

```json
"collision": "mesh"
```

根据模型网格生成三角面碰撞，适合建筑、地形、复杂障碍。

```json
"collision": "box",
"size": [4.0, 3.0, 6.0]
```

用盒子碰撞代替模型碰撞，性能更好，也更不容易卡角色。

```json
"collision": "none"
```

只有显示效果，没有碰撞。

---

## 4. 通用字段

所有物体都支持：

```json
{
  "name": "ObjectName",
  "position": [0.0, 0.0, 0.0],
  "rotation_degrees": [0.0, 0.0, 0.0],
  "scale": [1.0, 1.0, 1.0],
  "visible": true,
  "collision": true
}
```

说明：

- `rotation_degrees`：角度旋转。
- `scale`：缩放。
- `visible: false`：只保留碰撞，不显示模型，适合隐形碰撞体。
- `collision: false` 或 `collision: "none"`：不生成碰撞。

---

## 5. 一个完整自定义地图示例

```json
{
  "format_version": 1,
  "name": "小型测试地图",
  "runner_spawn": [-10.0, 0.12, 10.0],
  "tagger_spawn": [10.0, 0.12, -10.0],
  "objects": [
    {
      "type": "box",
      "name": "Ground",
      "position": [0.0, -0.15, 0.0],
      "size": [40.0, 0.3, 40.0],
      "color": [0.16, 0.18, 0.2]
    },
    {
      "type": "box",
      "name": "Ramp",
      "position": [-8.0, 1.3, -5.0],
      "size": [8.0, 0.5, 10.0],
      "rotation_degrees": [16.0, 0.0, 0.0],
      "color": [0.35, 0.38, 0.42]
    },
    {
      "type": "box",
      "name": "Platform",
      "position": [-8.0, 3.0, 1.0],
      "size": [9.0, 0.5, 5.0],
      "color": [0.25, 0.28, 0.33]
    },
    {
      "type": "cylinder",
      "name": "RoundPillar",
      "position": [6.0, 1.5, 2.0],
      "radius": 0.8,
      "height": 3.0,
      "color": [0.42, 0.42, 0.48]
    },
    {
      "type": "box",
      "name": "RunnerPad",
      "position": [-10.0, 0.03, 10.0],
      "size": [4.0, 0.08, 4.0],
      "color": [0.0, 0.32, 0.75]
    },
    {
      "type": "box",
      "name": "TaggerPad",
      "position": [10.0, 0.03, -10.0],
      "size": [4.0, 0.08, 4.0],
      "color": [0.75, 0.05, 0.02]
    }
  ]
}
```

---

## 6. 把地图加入官方地图列表

创建新的 JSON 地图文件后，把它放到：

```text
res://maps/
```

然后在 `res://scripts/game.gd` 顶部的 `OFFICIAL_MAPS` 里加入一项：

```gdscript
{
	"name": "新官方地图名",
	"path": "res://maps/your_map.json",
	"description": "这张地图的玩法说明。"
}
```

这样它就会出现在游戏内的 **官方地图列表** 里。联机时房主选择地图后，客户端会自动同步同一个官方地图索引。

---

## 7. 地图制作建议

建议地图里至少有：

- 2 ~ 3 个斜坡
- 6 ~ 12 个低障碍
- 4 ~ 8 个高挡板
- 若干柱子或绕行障碍
- 1 ~ 2 个高平台

为了减少卡住：

- 主通道宽度建议大于 `2.5`。
- 低障碍高度推荐 `1.0 ~ 1.2`。
- 高平台高度尽量控制在 `2.5 ~ 3.5`。
- 平台至少留一个斜坡可以上去。
- 复杂模型建议优先用 `collision: "box"` 或额外放几个隐形盒子做简化碰撞。

---

## 8. 测试清单

改完地图后测试：

- 玩家和抓人者是否出生在地面上。
- 斜坡是否能走上去。
- 低障碍是否能翻越。
- 抓人者是否能正常追人。
- 逃跑者掉出地图是否判负。
- 抓人者掉出地图是否回出生点。
- 联机双方 HUD 显示的地图名是否一致。
