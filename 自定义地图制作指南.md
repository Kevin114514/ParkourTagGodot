# 跑酷抓人：自定义地图制作指南

## 1. 当前地图是什么格式？

当前地图不是独立的 `.tscn`、`.json` 或外部地图文件，而是 **程序化 GDScript 地图**。

地图写在：

`res://scripts/game.gd`

主要函数是：

- `_build_arena()`：创建整张地图
- `_add_box()`：创建一个带碰撞体的长方体障碍

当前地图由大量 `StaticBody3D + CollisionShape3D + BoxMesh` 组成，也就是一堆可碰撞的盒子。斜坡也是通过旋转盒子实现的。

---

## 2. 地图物体的基本写法

在 `game.gd` 的 `_build_arena()` 里添加一行：

```gdscript
_add_box("物体名字", Vector3(x, y, z), Vector3(宽, 高, 长), Color(r, g, b), Vector3(旋转X, 旋转Y, 旋转Z))
```

最后一个 `rotation` 参数可以省略，省略时表示不旋转：

```gdscript
_add_box("Box1", Vector3(0.0, 0.5, 0.0), Vector3(4.0, 1.0, 4.0), Color(0.4, 0.4, 0.4))
```

---

## 3. 参数说明

### `name`

物体名字，方便调试用：

```gdscript
"Ground"
"RampA"
"VaultBlock1"
```

### `position: Vector3(x, y, z)`

物体中心点位置。

Godot 3D 坐标规则：

- `x`：左右
- `y`：高度
- `z`：前后

例如：

```gdscript
Vector3(0.0, 0.5, 0.0)
```

表示物体中心在地图中间，高度为 `0.5`。

注意：盒子的 `position.y` 是中心高度，不是底部高度。

如果盒子高度是 `1.0`，中心 `y = 0.5`，底部就在 `0.0`。

---

### `size: Vector3(宽, 高, 长)`

盒子的尺寸：

```gdscript
Vector3(4.0, 1.0, 2.0)
```

表示：

- 宽度 `4.0`
- 高度 `1.0`
- 长度 `2.0`

---

### `color: Color(r, g, b)`

颜色，数值范围是 `0.0 ~ 1.0`。

```gdscript
Color(0.18, 0.42, 0.46)
```

---

### `rotation: Vector3(x, y, z)`

旋转角度，单位是弧度。

通常用 `deg_to_rad()` 把角度转换成弧度：

```gdscript
Vector3(deg_to_rad(18.0), 0.0, 0.0)
```

常见用法：

```gdscript
# 斜坡
_add_box("Ramp", Vector3(0.0, 1.2, 0.0), Vector3(8.0, 0.5, 10.0), Color(0.35, 0.38, 0.42), Vector3(deg_to_rad(18.0), 0.0, 0.0))

# 旋转挡板
_add_box("Wall", Vector3(5.0, 1.0, 0.0), Vector3(1.0, 2.0, 8.0), Color(0.48, 0.29, 0.16), Vector3(0.0, deg_to_rad(20.0), 0.0))
```

---

## 4. 常见地图元素写法

### 地面

```gdscript
_add_box("Ground", Vector3(0.0, -0.15, 0.0), Vector3(64.0, 0.3, 64.0), Color(0.16, 0.18, 0.2))
```

### 围墙

```gdscript
_add_box("NorthWall", Vector3(0.0, 1.6, -32.0), Vector3(64.0, 3.2, 0.8), Color(0.09, 0.1, 0.12))
_add_box("SouthWall", Vector3(0.0, 1.6, 32.0), Vector3(64.0, 3.2, 0.8), Color(0.09, 0.1, 0.12))
_add_box("WestWall", Vector3(-32.0, 1.6, 0.0), Vector3(0.8, 3.2, 64.0), Color(0.09, 0.1, 0.12))
_add_box("EastWall", Vector3(32.0, 1.6, 0.0), Vector3(0.8, 3.2, 64.0), Color(0.09, 0.1, 0.12))
```

### 斜坡

```gdscript
_add_box("Ramp", Vector3(-10.0, 1.5, 0.0), Vector3(8.0, 0.5, 10.0), Color(0.35, 0.38, 0.42), Vector3(deg_to_rad(18.0), 0.0, 0.0))
```

建议斜坡角度控制在 `10° ~ 25°`，太陡会影响追人者寻路和玩家移动。

### 平台

```gdscript
_add_box("Platform", Vector3(-10.0, 3.0, 6.0), Vector3(10.0, 0.5, 5.0), Color(0.25, 0.28, 0.33))
```

### 可翻越低障碍

```gdscript
_add_box("VaultBlock", Vector3(5.0, 0.55, 5.0), Vector3(3.2, 1.1, 1.4), Color(0.18, 0.42, 0.46))
```

逃跑者的翻越检测比较适合 `1.0 ~ 1.2` 高的障碍。

### 高挡板

```gdscript
_add_box("TallWall", Vector3(0.0, 1.0, 10.0), Vector3(1.2, 2.0, 8.0), Color(0.48, 0.29, 0.16))
```

### 柱子

```gdscript
_add_box("Pillar", Vector3(8.0, 1.25, -8.0), Vector3(1.8, 2.5, 1.8), Color(0.42, 0.42, 0.48))
```

---

## 5. 创建自定义地图的推荐步骤

### 第一步：打开地图函数

打开：

`res://scripts/game.gd`

找到：

```gdscript
func _build_arena() -> void:
```

这个函数里面的 `_add_box()` 就是当前地图。

---

### 第二步：保留基础结构

建议保留：

- 地面 `Ground`
- 四面墙 `NorthWall` / `SouthWall` / `WestWall` / `EastWall`
- 出生点标记 `StartPad` / `TaggerPad`

可以先只改中间的障碍、斜坡、平台。

---

### 第三步：添加障碍

例如添加一个跑酷区：

```gdscript
_add_box("CustomRamp1", Vector3(-12.0, 1.4, -8.0), Vector3(8.0, 0.5, 10.0), Color(0.35, 0.38, 0.42), Vector3(deg_to_rad(16.0), 0.0, 0.0))
_add_box("CustomPlatform1", Vector3(-12.0, 3.0, -2.0), Vector3(9.0, 0.5, 5.0), Color(0.25, 0.28, 0.33))
_add_box("CustomVault1", Vector3(-4.0, 0.55, 4.0), Vector3(3.0, 1.1, 1.4), Color(0.18, 0.42, 0.46))
_add_box("CustomWall1", Vector3(6.0, 1.0, 8.0), Vector3(1.2, 2.0, 8.0), Color(0.48, 0.29, 0.16), Vector3(0.0, deg_to_rad(20.0), 0.0))
```

---

### 第四步：调整出生点

当前角色出生点不由 `StartPad` / `TaggerPad` 自动决定，而是在代码里写死：

```gdscript
Vector3(-23.0, 0.12, 22.0) # 逃跑者出生点
Vector3(23.0, 0.12, -22.0) # 抓人者出生点
```

如果要改出生点，需要修改：

- `_spawn_single_characters()`
- `_spawn_network_characters()`

同时建议把地图上的 `StartPad` 和 `TaggerPad` 移到相同位置，方便看见出生点。

---

## 6. 地图设计建议

### 逃跑者体验

建议地图里至少有：

- 2 ~ 3 个斜坡
- 6 ~ 12 个低障碍
- 4 ~ 8 个高挡板
- 若干柱子或绕行障碍
- 1 ~ 2 个高平台

### 抓人者体验

不要让地图出现太多完全封闭的死角，否则抓人者容易卡住。

建议：

- 平台至少有一个斜坡能上去
- 斜坡角度不要太大，推荐 `10° ~ 25°`
- 高平台高度尽量控制在 `2.5 ~ 3.5`
- 低障碍高度推荐 `1.0 ~ 1.2`
- 主要通道宽度建议大于 `2.5`

### 避免的问题

尽量避免：

```gdscript
# 太陡，不推荐
Vector3(deg_to_rad(45.0), 0.0, 0.0)
```

```gdscript
# 太窄，角色容易卡
Vector3(0.5, 3.0, 10.0)
```

```gdscript
# 平台太高但没有斜坡，抓人者难以上去
_add_box("HighPlatform", Vector3(0.0, 6.0, 0.0), Vector3(8.0, 0.5, 8.0), Color(0.3, 0.3, 0.3))
```

---

## 7. 一个完整自定义地图示例

可以把 `_build_arena()` 中间障碍部分替换成类似下面的内容：

```gdscript
func _build_arena() -> void:
	_add_box("Ground", Vector3(0.0, -0.15, 0.0), Vector3(64.0, 0.3, 64.0), Color(0.16, 0.18, 0.2))
	_add_box("NorthWall", Vector3(0.0, 1.6, -32.0), Vector3(64.0, 3.2, 0.8), Color(0.09, 0.1, 0.12))
	_add_box("SouthWall", Vector3(0.0, 1.6, 32.0), Vector3(64.0, 3.2, 0.8), Color(0.09, 0.1, 0.12))
	_add_box("WestWall", Vector3(-32.0, 1.6, 0.0), Vector3(0.8, 3.2, 64.0), Color(0.09, 0.1, 0.12))
	_add_box("EastWall", Vector3(32.0, 1.6, 0.0), Vector3(0.8, 3.2, 64.0), Color(0.09, 0.1, 0.12))

	_add_box("RampLeft", Vector3(-18.0, 1.4, -10.0), Vector3(8.0, 0.5, 10.0), Color(0.35, 0.38, 0.42), Vector3(deg_to_rad(16.0), 0.0, 0.0))
	_add_box("PlatformLeft", Vector3(-18.0, 3.0, -4.0), Vector3(10.0, 0.5, 5.0), Color(0.25, 0.28, 0.33))

	_add_box("RampRight", Vector3(18.0, 1.4, 10.0), Vector3(8.0, 0.5, 10.0), Color(0.35, 0.38, 0.42), Vector3(deg_to_rad(-16.0), 0.0, 0.0))
	_add_box("PlatformRight", Vector3(18.0, 3.0, 4.0), Vector3(10.0, 0.5, 5.0), Color(0.25, 0.28, 0.33))

	for i in range(8):
		var x := -20.0 + i * 6.0
		var z := 0.0 if i % 2 == 0 else 8.0
		_add_box("CustomWall%d" % i, Vector3(x, 1.0, z), Vector3(1.2, 2.0, 7.0), Color(0.48, 0.29, 0.16), Vector3(0.0, deg_to_rad(12.0), 0.0))

	for i in range(10):
		var x := -24.0 + (i % 5) * 12.0
		var z := -22.0 + int(i / 5) * 44.0
		_add_box("CustomVault%d" % i, Vector3(x, 0.55, z), Vector3(3.2, 1.1, 1.4), Color(0.18, 0.42, 0.46))

	_add_box("StartPad", Vector3(-23.0, 0.03, 22.0), Vector3(4.0, 0.08, 4.0), Color(0.0, 0.32, 0.75))
	_add_box("TaggerPad", Vector3(23.0, 0.03, -22.0), Vector3(4.0, 0.08, 4.0), Color(0.75, 0.05, 0.02))
```

---

## 8. 测试清单

改完地图后建议测试：

- 玩家能否从出生点正常落地
- 抓人者能否从出生点正常追人
- 斜坡是否能走上去
- 低障碍是否能翻越
- 是否存在一跳就掉出地图的地方
- 逃跑者掉出地图是否判负
- 抓人者掉出地图是否回出生点
- 联机时两边地图是否一致

注意：联机时双方必须使用同一个打包版本，否则地图不一致会导致位置和碰撞不同步。
